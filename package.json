{
  "name": "musicrate",
  "private": true,
  "version": "1.0.0",
  "description": "MUSICRATE — a closed music platform for artists and listeners. Premium mobile-first UX, real audio uploads, public 0–90 ratings.",
  "type": "module",
  "engines": {
    "node": ">=20.19"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -k -n api,web -c cyan,magenta \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client && npm run build -w server",
    "start": "npm run start -w server",
    "typecheck": "npm run typecheck -w client && npm run typecheck -w server",
    "test": "npm run test -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
